#include "../../include/list.h"

void print_int(struct s_node* node)
{
	if(node != NULL)
	{
		if(node != NULL)
		{
			int i = (int)*(int *)(node -> elem);
			my_int(i);
		}
	}
	else
	{
		//my_str("Error in print_int");
	}
}
