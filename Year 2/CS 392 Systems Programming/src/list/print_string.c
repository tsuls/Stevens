#include "../../include/list.h"

void print_string(struct s_node* node)
{
	if(node != NULL)
	{
		if(node != NULL)
		{
			char *printing = (char *)node -> elem;
			my_str(printing);
		}
	}
	else
	{
		//my_str("Error in print_string");
	}
}
